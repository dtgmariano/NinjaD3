/*var canvas = d3.select("body").append("svg")
               .attr("width", 1000)
               .attr("height",1000);

var group = canvas.append("g")
                  .attr("transform", "translate(300, 300)");

var r = 150;

var p = Math.PI * 2;

var arc = d3.svg.arc()
                .innerRadius(r - 20)
                .outerRadius(r)
                .startAngle(0)
                .endAngle(p*3/4)
                .cornerRadius(10);

var arc2 = d3.svg.arc()
                .innerRadius(r/2)
                .outerRadius(0)
                .startAngle(0)
                .endAngle(p);

group.append("path")
     .attr("d", arc); */